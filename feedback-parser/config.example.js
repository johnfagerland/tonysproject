// Copy this file to one named exactly:  config.js  (same folder as the page)
// and fill in ONE of the two options below. config.js is ignored by git so
// the key never ends up in the repo.

window.FEEDBACK_PARSER_CONFIG = {
  // OPTION A - local file mode: paste the API key John gave you.
  apiKey: "",

  // OPTION B - hosted mode: John's Vercel proxy URL (leave apiKey empty).
  // proxyUrl: "https://feedback-parser-proxy.vercel.app/api/parse",

  // Optional: model override.
  // model: "claude-opus-4-8",
};
