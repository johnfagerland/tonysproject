# Copy this file to one named exactly:  .env
# and paste the API key John gave you after the = sign (no quotes, no spaces).
ANTHROPIC_API_KEY=

# Optional: override the model (default is claude-opus-4-8).
# STORY_ASSIST_MODEL=claude-opus-4-8
